/* eslint-disable */
const colors = [
  0, 0, 0,       // EMPTY
  220, 220, 220, // WALL
  194, 178, 128, // SAND
  128, 197, 222, // WATER
  50, 205, 50,   // PLANT
  170, 16, 0,    // FIRE
];

const particleColors = new Uint8Array(colors);

export default particleColors;
